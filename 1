@extends('layouts.app')

@section('content')

<div class="container py-5">

    <h1 class="text-center fw-bold mb-5">Συμπλήρωση Εγγράφων</h1>

    @if(session('success'))
        <div class="alert alert-success text-center">{{ session('success') }}</div>
    @endif

    <div class="row gx-5 gy-4">

        <!-- Εισερχόμενα Έγγραφα -->
        <div class="col-md-6">
            <div class="card shadow-sm rounded-4 border-0">
                <div class="card-header bg-primary text-white text-center fs-5 fw-bold rounded-top">
                    Εισερχόμενα Έγγραφα
                </div>
                <div class="card-body">
                    <form action="/documents/incoming" method="POST">
                        @csrf
                        @include('documents._incoming_fields')
                        <button type="submit" class="btn btn-primary w-100 py-2 fw-bold mt-3">
                            Αποθήκευση Εισερχομένου
                        </button>
                    </form>
                </div>
            </div>
        </div>

        <!-- Εξερχόμενα Έγγραφα -->
        <div class="col-md-6">
            <div class="card shadow-sm rounded-4 border-0">
                <div class="card-header bg-success text-white text-center fs-5 fw-bold rounded-top">
                    Εξερχόμενα Έγγραφα
                </div>
                <div class="card-body">
                    <form action="/documents/outgoing" method="POST">
                        @csrf
                        @include('documents._outgoing_fields')
                        <button type="submit" class="btn btn-success w-100 py-2 fw-bold mt-3">
                            Αποθήκευση Εξερχομένου
                        </button>
                    </form>
                </div>
            </div>
        </div>

    </div>

</div>

@endsection
